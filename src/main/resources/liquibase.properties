# --- Database Connection ---
url=jdbc:postgresql://localhost:5432/mediroute
username=postgres
password=your_password
driver=org.postgresql.Driver

# --- Liquibase Changelog Master ---
changeLogFile=src/main/resources/db/changelog/db.changelog-master.xml

# --- Hibernate Dialect (Optional if needed) ---
# If you want to generate diff from JPA entities
# referenceUrl=hibernate:spring:com.mediroute.entity?dialect=org.hibernate.dialect.PostgreSQLDialect

# --- Logging (Optional) ---
logLevel=info


# ============================================================================
# LIQUIBASE CONFIGURATION
# ============================================================================

contexts=dev,prod
